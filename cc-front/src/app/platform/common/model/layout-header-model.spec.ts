import { LayoutHeaderModel } from './layout-header-model';

describe('LayoutHeaderModel', () => {
  it('should create an instance', () => {
    expect(new LayoutHeaderModel()).toBeTruthy();
  });
});
