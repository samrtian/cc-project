import { ContainerCssDirective } from './container-css.directive';

describe('ContainerCssDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainerCssDirective();
    expect(directive).toBeTruthy();
  });
});
