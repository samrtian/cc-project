import { DataDicPipePipe } from './data-dic-pipe.pipe';

describe('DataDicPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DataDicPipePipe();
    expect(pipe).toBeTruthy();
  });
});
