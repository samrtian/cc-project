import { ExpressionExePipePipe } from './expression-exe-pipe.pipe';

describe('ExpressionExePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExpressionExePipePipe();
    expect(pipe).toBeTruthy();
  });
});
