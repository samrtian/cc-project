import { ErrorSrcDirective } from './error-src.directive';

describe('ErrorSrcDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorSrcDirective(null);
    expect(directive).toBeTruthy();
  });
});
