import { PageToolDirective } from './page-tool.directive';

describe('PageToolDirective', () => {
  it('should create an instance', () => {
    const directive = new PageToolDirective();
    expect(directive).toBeTruthy();
  });
});
