import { ScrollbarDirective } from './scrollbar.directive';

describe('ScrollbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollbarDirective(null);
    expect(directive).toBeTruthy();
  });
});
