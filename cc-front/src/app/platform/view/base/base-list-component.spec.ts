import { BaseListComponent } from './base-list-component';

describe('BaseListComponent', () => {
  it('should create an instance', () => {
    expect(new BaseListComponent()).toBeTruthy();
  });
});
